{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for user in users %}
            <li><a href="{{ path('game_start', { 'user_id': user.id, 'category_id': category.id }) }}">{{ user.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}